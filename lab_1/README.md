# Технологии и методы программирования

## Лабораторная работа №1: «Инструменты контроля качества и CI»

В рамках данной лабораторной работы требуется продемонстрировать умение настраивать инструменты автоматического контроля качества кода и автоматизировать типовые операции проекта.

В качестве основы необходимо использовать **существующий проект на Python**, в котором не настроен CI и прочие инструменты автоматической проверки кода.

В результате выполнения лабораторной работы проект должен получить единый набор команд для разработки, автоматические проверки перед коммитом и/или отправкой изменений, а также, по желанию (плюс в карму), автоматическую проверку пул-реквестов через GitHub Actions.

## Задание

1. Создать отдельную ветку в своем [форке](https://docs.github.com/en/get-started/quickstart/fork-a-repo) данного репозитория.

2. Выбрать существующий код / репозиторий на Python, с которым будете работать:

   * свой старый код;
   * код другого студента;
   * найденный в инитернете;

3. Настроить **линтер** для проекта. Рекомендуется использовать [Ruff](https://docs.astral.sh/ruff/).

   * добавить конфигурацию линтера в проект;
   * определить набор проверяемых правил;
   * исправить найденные проблемы.

4. Настроить **форматирование кода** с помощью [Ruff](https://docs.astral.sh/ruff/).

   * настроить параметры форматирования;
   * обеспечить возможность проверить форматирование (`ruff format --check`);
   * обеспечить возможность автоматически отформатировать проект.

5. Настроить **статическую проверку типов**. Рекомендуется использовать [Pyright](https://microsoft.github.io/pyright/).

   * добавить конфигурацию тайпчекера;
   * настроить уровень строгости проверки;
   * устранить найденные ошибки типизации.

6. Создать **Makefile** и в него прописать все полезные команды / шорткаты для часто выполняемых операций в проекте.

   Например:

   ```text
   make run
   make test
   make build
   make deploy
   ```

   Конкретный набор команд может отличаться в зависимости от особенностей проекта.

7. Прописать таргеты для запуска линтеров/тайпчекеров.

   Например:

   * `make lint` — запуск линтера;
   * `make format` — форматирование проекта;
   * `make typecheck` — проверка типов;
   * `make fullcheck` — запуск всех статических проверок;
   * `make test` — запуск тестов;
   * `make fulltest` — запуск статических проверок и всех тестов.

   Конкретные имена и состав таргетов могут отличаться, в зависимости от вашего проекта и предпочтений

8. Настроить **Git hooks** с помощью [pre-commit](https://pre-commit.com/).

   Необходимо:

   * добавить файлы с конфигурацией для pre-commit;
   * настроить проверки, выполняемые перед созданием коммита (pre-commit);
   * настроить проверки, выполняемые перед отправкой изменений в удаленный репозиторий (pre-push);
   * добавить команду для установки хуков, например:

   ```text
   make precommit
   ```

9. Рекомендуется дополнительно настроить **GitHub Actions** для автоматической проверки пул-реквестов.

В рамках дополнительной части можно настроить workflow, который при создании или обновлении PR автоматически запускает:

* линтер;
* проверку форматирования;
* проверку типов;
* тесты.

12. Открыть [пул-риквест](https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/proposing-changes-to-your-work-with-pull-requests/creating-a-pull-request-from-a-fork) в иcходный репозиторий и ждать ревью.

## Условия сдачи

* Работа выполняется в отдельной ветке форка исходного репозитория.
* В проекте присутствует рабочая конфигурация линтера, форматтера и тайпчекера.
* В проекте присутствует **Makefile** с командами для основных операций.
* В Makefile присутствуют команды для запуска автоматических проверок.
* Все основные проверки можно запустить без ручного ввода длинных команд.
* В проекте присутствует конфигурация **pre-commit**.
* Настроенные хуки действительно запускают соответствующие проверки.
* Присутствует команда `make precommit` (или аналогичная), устанавливающая необходимые хуки.
* В описании пул-реквеста указана ссылка на исходный проект, если использовался сторонний код.

## Ремарки

Работать с Git вы можете так, как вам удобно:

* через интерфейс [командной строки](https://git-scm.com/book/en/v2/Getting-Started-Installing-Git);
* через [плагин](https://www.jetbrains.com/help/pycharm/set-up-a-git-repository.html#fetch) в IDE;
* через десктопный клиент, типа [GitHub Desktop](https://desktop.github.com/).

Если вы столкнулись с непреодолимыми трудностями в ходе выполнения лабораторной работы, вы можете задать вопрос:

* в виде [ишью](https://github.com/itsecd/prog_instruments_labs/issues/new/choose) в этом репозитории;
* в разделе [Q&A](https://github.com/itsecd/prog_instruments_labs/discussions/categories/questions) дискуссий в этом репозитории;
* в телеграм-чате предмета;
* в телеграм-чате вашего курса.
